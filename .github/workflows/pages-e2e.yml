name: Pages Deploy and E2E

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - main
  schedule:
    - cron: '17 3 * * *'

permissions:
  contents: read
  pages: write
  id-token: write
  actions: read

concurrency:
  group: pages-deploy-and-e2e
  cancel-in-progress: false

jobs:
  deploy-and-e2e:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Run unit tests
        run: npm test

      - name: Install Playwright
        run: |
          npm install --no-save @playwright/test
          npx playwright install --with-deps chromium

      - name: Prepare Pages artifact
        run: |
          rm -rf dist
          mkdir -p dist
          cp index.html main.js styles.css dist/

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5

      - name: Wait for deployed Pages content
        env:
          E2E_BASE_URL: ${{ steps.deployment.outputs.page_url }}
        run: node scripts/wait-for-pages.mjs

      - name: Capture deployed Pages HTTP diagnostics
        env:
          E2E_BASE_URL: ${{ steps.deployment.outputs.page_url }}
        run: |
          set +e
          curl --include --location --max-time 30 "$E2E_BASE_URL" | head -200 | tee e2e-http-diagnostics.txt
          exit 0

      - name: Run deployed Pages E2E
        env:
          E2E_BASE_URL: ${{ steps.deployment.outputs.page_url }}
          PLAYWRIGHT_HTML_OPEN: never
        run: npx playwright test --reporter=line,html

      - name: Upload E2E diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-diagnostics
          path: |
            e2e-http-diagnostics.txt
            playwright-report/
            test-results/
          if-no-files-found: ignore
